@{
    ViewData["Title"] = "Access Denied";
    var isAuthenticated = User.Identity?.IsAuthenticated ?? false;
    var homeController = isAuthenticated ? "Home" : "Account";
    var homeAction = isAuthenticated ? "Index" : "Login";
}

<div class="d-flex justify-content-center align-items-center" style="height: 150vh;">
    <div class="text-center" style="position: relative; top: -150px;">
        <h1 class="text-danger">Access Denied</h1>
        <p>You do not have permission to access this page.</p>

        <a asp-controller="@homeController" asp-action="@homeAction" class="btn btn-primary btn-lg">
            @(isAuthenticated ? "Go Home" : "Login")
        </a>
    </div>
</div>

